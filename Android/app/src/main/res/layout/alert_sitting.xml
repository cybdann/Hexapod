<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@drawable/curve_shape"
    android:padding="5dp">

    <com.airbnb.lottie.LottieAnimationView
        android:layout_width="120dp"
        android:layout_height="120dp"
        app:lottie_rawRes="@raw/waiting"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        android:id="@+id/lottie_connection_error"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Waiting for the Hexapod"
        android:id="@+id/text_sitting_walking"
        android:layout_below="@+id/lottie_connection_error"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="20dp"
        android:textSize="20sp"
        android:textColor="#FAFAFA"
        android:layout_centerHorizontal="true"
        android:fontFamily="@font/azonix"
        android:gravity="center_horizontal" />

</RelativeLayout>